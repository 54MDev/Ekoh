# Used by the Partykit server (party/index.ts) and the standalone test script.
# Loaded by partykit dev from .env automatically.
OPENAI_API_KEY=sk-...

# Optional override for the local dev WebSocket host (defaults to localhost:1999).
# VITE_PARTYKIT_HOST=localhost:1999
